import Foundation

/// Single source of truth for app identity shown in the About panel and Settings.
enum AppInfo {
    static let name = "Nudgebar"
    static let version = "0.1.0"
    static let releaseDate = "June 1, 2026"
    static let repoURL = "https://github.com/mikec-git/nudgebar"
    static let repoLabel = "github.com/mikec-git/nudgebar"
}
